server {
	listen 80;
#	server_name   wanglian.love;
	#rewrite ^(.*)$ https://59.110.152.124;
	 location / {
        root   /html/love.wanglian;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}

server {
	listen       443 ssl;
	server_name  wanglian.love;
	#access_log  logs/host.access.log  main;

    # ssl config
    ssl_certificate      ./conf.d/love.wanglian_bundle.crt;
  	ssl_certificate_key  ./conf.d/love.wanglian.key;
    # ssl_session_timeout 5m;
    # #请按照以下协议配置
    # ssl_protocols TLSv1.2 TLSv1.3;
    # #请按照以下套件配置，配置加密套件，写法遵循 openssl 标准。
    # ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
    # ssl_prefer_server_ciphers on;

	# gzip config
	gzip on;
	gzip_min_length 1k;
	gzip_comp_level 9;
	gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
	gzip_vary on;
	gzip_disable "MSIE [1-6]\.";

    location / {
        root   /html/love.wanglian;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    location /api {
        # proxy_set_header Host $http_host;          //此处要添加host头，将头信息返回服务器
	    #设置主机头和客户端真实地址，以便服务器获取客户端真实IP
	    proxy_set_header Host $host;
	    proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header REMOTE-HOST $remote_addr;
	    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	    proxy_pass http://127.0.0.1:88;
    }

}

