server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native  # 明确指定使用native profile
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/config/
            - file:./config/
#        git:
#          uri: https://github.com/your-repo/config-repo
#          default-label: main
#          search-paths: '{application}'
#          username: ${GIT_USERNAME:your-username}
#          password: ${GIT_PASSWORD:your-token}
#          basedir: ./local-cache
#          timeout: 10

# 注册到 Eureka
eureka:
  client:
    enabled: false